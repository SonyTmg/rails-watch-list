<h1>Make a new Bookmark</h1>
<%= simple_form_for(@bookmark, url: list_bookmarks_path) do |f| %>
  <%= f.input :comment %>
  <%= f.association :list %>
  <%= f.association :movie %>
  <%= f.button :submit %>
<% end %>
