<h1>Lists</h1>
<div class="container">
  <%@lists.each do |list| %>
    <%=link_to list.name, list_path(id: list.id)  %>
  <% end %>
</div>

<p>
  <%= link_to 'Create new list', new_list_path %>
</p>
